# 贡献指南

感谢你对 picgo-plugin-watermark-pro 的关注！我们欢迎各种形式的贡献。

## 如何贡献

### 报告 Bug

如果你发现了 bug，请：

1. 在 [GitHub Issues](https://github.com/sysfox/picgo-plugin-watermark-pro/issues) 中搜索是否已有相关问题
2. 如果没有，创建新 issue，包含：
   - 清晰的标题和描述
   - 重现步骤
   - 期望的行为
   - 实际的行为
   - 你的环境（操作系统、Node.js 版本、PicGo 版本）
   - 如果可能，提供截图或错误日志

### 提出新功能

如果你有新功能的想法：

1. 先在 Issues 中讨论，确保这个功能符合项目方向
2. 说明功能的用途和使用场景
3. 如果可以，提供实现思路

### 提交代码

1. **Fork 仓库**

```bash
# 在 GitHub 上 fork 仓库，然后克隆你的 fork
git clone https://github.com/你的用户名/picgo-plugin-watermark-pro.git
cd picgo-plugin-watermark-pro
```

2. **创建分支**

```bash
git checkout -b feature/your-feature-name
# 或
git checkout -b fix/your-bug-fix
```

3. **进行修改**

- 遵循现有的代码风格
- 添加必要的测试
- 更新相关文档

4. **测试**

```bash
# 运行所有测试
npm test

# 运行 lint
npm run lint

# 本地测试
npm run dev:test
```

5. **提交**

```bash
git add .
git commit -m "feat: 添加新功能的描述"
# 或
git commit -m "fix: 修复某个 bug 的描述"
```

提交信息格式：

- `feat:` 新功能
- `fix:` 修复 bug
- `docs:` 文档更新
- `style:` 代码格式调整（不影响功能）
- `refactor:` 重构代码
- `test:` 添加或修改测试
- `chore:` 构建过程或辅助工具的变动

6. **推送并创建 Pull Request**

```bash
git push origin feature/your-feature-name
```

然后在 GitHub 上创建 Pull Request。

## 开发规范

### 代码风格

- 使用 2 空格缩进
- 使用单引号
- 不使用分号（除非必要）
- 遵循 ESLint 规则

### 测试要求

- 新功能必须添加测试
- bug 修复应该添加回归测试
- 测试覆盖率不应下降
- 所有测试必须通过

### 文档要求

- 新功能需要更新 README.md
- API 变更需要更新相关文档
- 添加 CHANGELOG.md 条目

## 测试指南

### 运行测试

```bash
# 所有测试
npm test

# 监听模式
npm run test:watch

# 覆盖率报告
npm run test:coverage
```

### 编写测试

测试文件位于 `test/` 目录，使用 Jest 框架。

示例：

```javascript
describe('功能模块', () => {
  test('应该做某件事', () => {
    // 测试代码
    expect(result).toBe(expected)
  })
})
```

### 本地测试

运行 `npm run dev:test` 会在 `test/` 目录生成带水印的测试图片，你可以直观地查看效果。

## 提交 Pull Request 的注意事项

- 确保所有测试通过
- 确保代码符合 ESLint 规则
- PR 描述清晰，说明改动内容和原因
- 如果解决了某个 issue，在 PR 中引用（如 `fixes #123`）
- 保持提交历史清晰，必要时进行 rebase
- 响应 review 意见

## 项目结构

```
picgo-plugin-watermark-pro/
├── src/
│   └── index.js              # 主要功能代码
├── test/
│   ├── local-test.js         # 本地快速测试
│   ├── watermark.test.js     # 单元测试
│   └── watermark-test-helper.js  # 测试辅助
├── scripts/
│   └── link-to-picgo.js      # 开发辅助脚本
├── .github/
│   └── workflows/            # CI/CD 配置
├── package.json              # 项目配置
├── .eslintrc.js              # ESLint 配置
└── README.md                 # 项目文档
```

## 发布流程（维护者）

1. 更新版本号和 CHANGELOG
2. 运行完整测试：`npm test`
3. 提交并打 tag：`git tag v1.x.x`
4. 推送到 GitHub：`git push && git push --tags`
5. GitHub Actions 自动发布到 npm

## 获得帮助

- 查看 [README.md](./README.md)
- 查看 [QUICKSTART.md](./QUICKSTART.md)
- 在 [Issues](https://github.com/sysfox/picgo-plugin-watermark-pro/issues) 提问
- 查看现有的 Pull Requests

## 行为准则

- 尊重所有贡献者
- 保持友好和建设性的讨论
- 接受建设性的批评
- 专注于对社区最有利的事情

## 许可证

通过贡献代码，你同意你的贡献将在 MIT 许可证下发布。

---

再次感谢你的贡献！ 🎉
